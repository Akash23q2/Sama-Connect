# Backend Server Configuration (Cloudflare tunnel)
VITE_BACKEND_HOST_IP=purse-headphones-percent-ascii.trycloudflare.com

# PeerJS Server Configuration (if needed)
VITE_PEERJS_HOST_IP=witch-charlie-liabilities-implications.trycloudflare.com

# Protocol Configuration
# true for HTTPS/WSS, false for HTTP/WS, or empty for auto-detect
VITE_IS_SECURE=true

# Protocol Configuration
# Set to 'true' to use HTTPS/WSS, 'false' for HTTP/WS, or leave empty for auto-detection
# # Auto-detection: uses HTTPS/WSS if frontend is served over HTTPS
# VITE_IS_SECURE=false

# Leave empty to auto-detect from current hostname
# Example: VITE_BACKEND_HOST_IP=192.168.43.178
# Example: VITE_PEERJS_HOST_IP=192.168.43.178
