# Backend Server Configuration
# Leave empty to auto-detect from current hostname
# For local network testing, set to your machine's local IP (e.g., 192.168.1.100)
VITE_BACKEND_HOST_IP=
# Protocol Configuration
# Set to 'true' to use HTTPS/WSS, 'false' for HTTP/WS, or leave empty for auto-detection
# Auto-detection: uses HTTPS/WSS if frontend is served over HTTPS
VITE_IS_SECURE=

# How to find your local IP:
# - Windows: ipconfig (look for IPv4 Address)
# - Mac/Linux: ifconfig or ip addr (look for inet address, usually starts with 192.168.x.x or 10.x.x.x)
